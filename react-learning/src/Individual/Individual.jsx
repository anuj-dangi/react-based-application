import styles from "./Individual.module.css";

function Individual()
{
    return (
        <h1 className={styles.text}>Styling through module in which class name is changed with hashcode</h1>
    );
};

export default Individual;